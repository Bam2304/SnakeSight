Everything is run from finalGui.py, however some modules will need to be installed using pip.

-Tkinter for gui

-Pillow for image processing

-supabase for backend database storage

-dotenv for backend storage as well

After these modules are installed, gui.py should be ready to run.
